def ord(medicina):
    sorted(medicina.split(),key=lambda palabra:len(palabra),reverse=True)
 

entrada = input().split
buscar = int(entrada[1])
medicina = [input()]

for i in range(buscar):
    med = input()
    if med in medicina:
        return posicion
    
   
ord(arr)
print(" ".join(x[0] for x in arr))
